# AA GUNMAN — 進行ログ

人間不在の自動開発。すべての仕様判断・数値決定はこのファイルに残す。

## 環境の確定事項

- Node v22.22.2 / npm 10.9.7 / Chromium は `/opt/pw-browsers`（playwright 1.56.1）
- React 18.3.1 系で固定。`@react-three/fiber@8.17.10` / `@react-three/drei@9.114.3` /
  `@react-three/rapier@1.5.0` / `@react-three/postprocessing@2.16.3` / `three@0.168.0`。
  最新版は React 19 を要求して ERESOLVE で落ちるため、すべて完全固定（`^` なし）。
- `npm create vite` は使わず設定ファイルを手書き。

---

## 0. 土台 — 完了 (09/17)

### verify の設計

`npm run verify` は Playwright(chromium, SwiftShader) で production build を開き、
シナリオごとに以下を判定する。

1. コンソールエラー / 未捕捉例外 / リクエスト失敗が **0件**
2. スクリーンショットを `shots/` に保存
3. フレームの **輝度標準偏差** が閾値以上（単色に近い画面を失敗とする）

平均輝度で判定しない理由は実測で裏付けが取れた。後述の意図的破壊テスト3で
「アリーナを描画しない」状態を作ったとき、**std=1.13 / mean=4.4** だった。
mean は真っ黒(0)でも真っ白(255)でもないので、平均基準のゲートはこれを通してしまう。
std は6色しか存在しない空フレームを正しく弾いた。

シナリオ: `01-title` / `02-arena` / `03-charge-volt` / `04-charge-amp`。
02以降は実際にDRAWを押してポインタロックを取り、マウスボタンを押して離すという
本番と同じ経路を通す。

verify は毎回ランダムな空きポート(4300-4899)を使う。理由: 最初の実装で固定ポート
4173 を使ったところ、中断した前回の preview サーバが生き残って **古い dist を配信し、
verify が嘘をつく**事故が起きた。ポート固定は検証の信頼性を壊す。

### 意図的破壊テスト（3件すべて exit 1 を確認）

| 仕込んだ故障 | 結果 |
|---|---|
| 1. マウント時に例外を投げる | canvas が出ず timeout → exit 1 |
| 2. canvas は正常、console.error を1回出すだけ | std=18 と健全なのにエラー検出で exit 1 |
| 3. エラーゼロ、Arena を描画しない | std=1.13 で exit 1（mean=4.4 なので平均基準なら見逃していた）|

### 実装中に見つかった実バグ（検証が拾った）

- **カメラブームの自己衝突**: 壁めり込み防止のレイキャストが自分のカプセルに当たり、
  カメラが常に頭から0.7mの位置に貼り付いていた。`QueryFilterFlags.EXCLUDE_DYNAMIC`
  で解決（アリーナは全て fixed、動的剛体はプレイヤーのみ）。
- **ポインタロック時のカーソルワープ**: Chrome はロック中にカーソルを原点へ戻し、
  その**ワープ自体を movementX/Y として通知**する。実測すると「本物の移動 (640,478)」
  の直後に「その正確な符号反転 (-640,-478)」が届く。素通しするとDRAWを押した瞬間に
  カメラが空を向く（実際に verify のスクショが空だけになった）。
  対策は2段: ①1イベントで320pxを超える移動は破棄（人間の手では出ない）
  ②直前イベントの完全な符号反転は破棄（本物のマウスは大きな移動を次サンプルで
  完璧に打ち消さない）。どちらも実機の入力を壊さない。

### 決めた構成上の判断

- **物理は「プレイヤー＋アリーナ」だけ Rapier**。弾と敵は自前ロジックにする。
  - 弾: 剛体にすると高速時にトンネリングし、貫通の制御も難しい。毎フレーム
    線分レイで進める方式なら貫通数を正確に扱える。
  - 敵: 20体以上を剛体にすると押し合いのジャンクが増える。距離ベースの分離操舵で十分。
- **60Hz で動く状態は React の外**（`src/game/runtime.ts`）。zustand には HUD が
  見る粗い情報だけを入れ、チャージ値のような毎フレーム変化する値は入れない。

---

## 2. 溜め撃ち — 完了 (09/17)

### V/A → 弾 の数式（確定）

チャージ量 `vRaw`,`aRaw` (0..1) をフロア付きで実効値に写す。
`v = 0.10 + vRaw*0.90`, `a = 0.08 + aRaw*0.92`（タップでも使える弾が出る）。

| | 値域 | 由来 |
|---|---|---|
| ボルト V | 20 → 400 V | 押し出す力 |
| アンペア A | 0.5 → 10 A | 流す量 |
| 弾速 | 44 → 205 m/s | V |
| 射程 | 22 → 92 m | V |
| 貫通数 | 0 → 4 | V |
| ダメージ | A × 12 | A |
| 感電半径 | aRaw × 6.5 m | A |
| 感電ダメージ | A × 5（距離減衰）| A |
| スタン | aRaw × 0.9 s | A |
| **電池消費** | **V × A / 40** | 積 |

### なぜ「常時フルチャージ」が最適にならないか

消費が積なので、**特化した弾は安く、混ぜた弾だけが高い**。

| 撃ち方 | V | A | 消費 | 用途 |
|---|---|---|---|---|
| ボルト全振り (1,0) | 400 | 1.26 | **12.6** | 貫通4・装甲抜き・205m/s |
| アンペア全振り (0,1) | 58 | 10.0 | **14.5** | 半径6.5mの感電・群れ消し |
| 両方全開 (1,1) | 400 | 10.0 | **100** | 1シリンダー(600)の1/6を1発で |

両方全開は特化弾の **約7倍**のコストで、得られるのは「貫通と感電の同時」だけ。
装甲個体は 220V 未満を弾き、群れ個体は感電でしか割に合わず、高速個体は
溜めている時間そのものが損になる。つまり**3種の敵がそれぞれ別の特化弾を要求する**。
この構造を verify の 06/07/08 が数値で固定している（許容誤差 ±0.4 units）。

### シリンダーと電池

6室 × 100 units = 600。消費は室0から順に溢れながら引く。
最大出力弾がちょうど1本の単三を空にする、という対応で消費量を体感させる。
足りないときは撃てないのではなく**出力が落ちる**（`scaleSpec`: V,A を √power 倍）。
詰みを作らないため。

### 発射ルール（仕様どおり、変更禁止事項）

- 右長押し → V上昇＋FOVが74°→26°に収束（**これがVのゲージ。バーは出さない**）
- 左長押し → A上昇＋周囲の放電が激しくなる（**これがAのゲージ**）
- **どちらのボタンも押されていない状態に戻った瞬間に発射**。離す順序は不問。
- verify 03/04/05 が「押している間は絶対に撃たない」を毎回確認している。

### 検証で見つかった実バグ

- **dt クランプが 1/30 だった**。30fps 未満の環境では、チャージもクールダウンも
  エフェクト寿命も**静かにスローモーションになる**。SwiftShader 実測 6.9fps で
  1.35秒のチャージが 5.9秒かかっていた。クランプは本来「タブ切替やGC停止を
  生き延びる」ためのもので、0.2秒まで引き上げた。弾は1ステップ全体をレイで
  掃くのでコマ落ちしてもすり抜けない。
- **teleport フィルタが Playwright の正当な移動も食っていた**。320px 閾値は
  実機では正しいが、CDP の合成イベントは1回で700px動く。検証側は「カメラを
  動かして狙う」のをやめ、**チャージ完了をポーリングして待つ**方式に変更した。
  壁時計待ちは 4-7fps では意味を持たないため、これは検証の質としても改善。
- **時間依存アサーションをやめた**。「今この瞬間に弾が画面にある」はフレームレート
  との競争になる。代わりに累積カウンタ（`stats.boltsSpawned` など）を置き、
  「1発生成され、1発が着弾か射程切れで解決した」を確認する。こちらが本来の不変条件。

### 描画の決定

パーティクルは全て加算合成のインスタンスメッシュ。**フェードをアルファではなく
インスタンスカラーの明度で表現**している（加算では黒＝透明なので、半透明ソートが
まるごと不要になる）。グローは板テクスチャではなく「中心が白・外周が黒」の頂点カラー
を持つ扇形ジオメトリで、外部素材ゼロで柔らかい光になる。

---

## 3. 敵・当たり判定・撃破・プレイヤーHP — 完了 (09/17)

### 敵を物理剛体にしなかった理由

- 20体以上の近接敵を剛体にすると押し合いのジャンクが支配的になる
- 弾の貫通は「到達順に処理する」必要があり、接触イベント方式では順序が保証されない

よって敵は純粋なデータ配列にして、操舵（追尾＋敵同士の分離＋鉄骨の回避）で動かす。
弾との判定は**線分と球の交差**で、1フレーム分の移動区間をまるごと掃く。
弾速205m/sでコマ落ちしてもすり抜けない。

### 命中処理の順序（確定）

1. そのフレームの移動区間に入った敵を全部集め、**到達距離の近い順にソート**
2. 手前から順に処理し、装甲判定 → ダメージ → 感電 → 貫通残数を消費
3. 貫通が尽きたらそこで停止。世界（壁・地面）の当たりが敵より手前なら世界が勝つ

**感電は「最初に入った1体」でしか発動しない**（`arcSpent`）。
貫通しながら各敵で感電を撒くと、混合弾が高コストである意味が消えるため。

### verify に足した確認

- `10-arc-kill`: 群れ4体を正面に置き、アンペア全振り1発（14.5 units）で**4体とも倒れる**ことを確認。
  「群れは感電でまとめて処理」が数値で成立していることの固定。
- `11-player-damage`: 接触ダメージが HP に正しく反映され、
  無敵時間があるので `hp === 100 - 被弾数×6` が厳密に成り立つことを確認。
- `12-enemies-on-screen`: **敵を出す前後のフレームの平均絶対差**を測り、3以上を要求。
  「敵が存在して死ぬ」ことは assert で分かるが、「**描画されている**」ことは
  それでは分からない。何も描かない描画器はこの差分を偽造できない。

### 検証で見つかった問題

- **`10-arc-kill` が最初フレーキーだった**。敵の接近方向がランダムなので、
  「一番近い敵」が背後にいると正面に撃った弾が誰にも当たらない。
  検証用に `__aa.debugSpawnAhead()` を追加し、正面の決まった位置に置くようにした。
  これは Chrome のポインタロックが CDP の合成マウス入力でカメラを回せない
  （移動と同時に打ち消しのワープが届く）ことへの対処でもある。通常の spawn 経路を通る。
- **装甲個体が鉄骨と見分けがつかなかった**。暗い茶色の六角柱で、光るコアは
  不透明な胴体の内側に埋まって見えなかった。コアを**正面の表面**に出し、
  装甲だけ冷たい鋼色にして、暖色の背景から分離した。

---

## 4. 電池リソース — 完了 (09/17)

### シリンダーとリロードの規則（確定）

- 6室 × 100 units。消費は室0から順に、足りなければ次の室へ溢れて引く
- **リロードは「満タンでない室を丸ごと新品に差し替える」**。差し替えた室ごとに予備1本を消費
- したがって**残量のある室でリロードすると、その残量は捨てられる**

`14-reload-waste` がこの損を数値で固定している: 室0が40残っている状態でリロードすると、
予備を1本(=100)使って capacity は 60 しか増えない。**早すぎるリロードは電池を捨てる行為**であり、
「いつ入れ直すか」がリソース判断になる。

### 空撃ちにしない設計

シリンダーの残量が要求コストに足りないとき、**撃てない**のではなく**出力が落ちる**
（V と A をそれぞれ √power 倍 → V×A がちょうど power 倍）。
残り4 units でも弱い弾が出る。詰みを作らないため。
完全に 0 かつ予備も 0 になって初めて敗北（`OUT OF BATTERIES`）。

### HUD の方針

画面を埋めない。左下に「BODY バー」と「6本の電池」と「予備数」だけ。
右下は一時メッセージ5行まで。上中央はウェーブと残敵数。
**V/A のゲージは無い**（ズーム倍率と放電の強さがそれ）。
リロードの進捗と被弾フラッシュは毎フレーム更新なので、
zustand を経由せず `requestAnimationFrame` で直接 DOM を触っている（HUDの再描画を起こさない）。

銃のシリンダーの6本のセルは HUD の6室と1対1で、残量に応じて発光が落ちる。
リソースが武器の上でも読める。

---

## 5. 敵3種と撃ち分けの成立 — 完了 (09/17)

「撃ち分けないと electricity が尽きる」は感覚では確認できないので、
**実際の config と実際の弾道計算を使って (V,A) 平面を総当たりし、
各脅威を最安で処理する弾を求める解析**を `src/game/economy.ts` に置いた。
verify がこの出力に assert しているので、数値をいじって3つの答えが1つに
潰れたら（＝撃ち分けが成立しなくなったら）ビルドが落ちる。

### 求まった最適解（実測）

| 敵 | 最適 V | 最適 A | 電圧 | 電流 | コスト | 1体あたり |
|---|---|---|---|---|---|---|
| 装甲 | 0.48 | 0.68 | 222V | 7.2A | 40.0 | 40.0 |
| 群れ(5体密集) | **0.00** | 0.40 | 58V | 4.8A | 6.9 | **1.38** |
| 高速(単独) | **0.00** | 0.08 | 58V | 2.0A | 2.8 | 2.84 |

- 装甲は 220V の閾値を**ぎりぎり超える**ところが最適。過剰な電圧は無駄。
- 群れは**ボルトが完全に無価値**。感電半径が群れの密集半径(2.6m)に届く最小の A が最適。
  ここでも「A を全開にする」のは無駄。
- 高速個体はほぼタップ。溜めている時間そのものが損なので、これは時間的にも正しい。

最適解同士の (V,A) 平面上の最小距離は **0.32**。3つは明確に別の弾。

### ラン全体の収支（これが核心）

| | 必要量 |
|---|---|
| 全体の電池予算（シリンダー600 + 予備8本 + ウェーブ補給2本×5） | **2400** |
| 撃ち分けた場合の理論最小消費（6ウェーブ合計） | **620** |
| 全弾フルチャージで通した場合 | **4460** |

**フルチャージ連打は予算の 1.86 倍を要求する**（＝ウェーブ4あたりで電池が尽きる）。
撃ち分けた場合は予算の 26% で足り、**3.87倍の余裕**が残る。
この余裕が「外す・溜めすぎる」ための現実的なマージンになる。

この結果を出すために予備電池を 18本→8本、ウェーブ補給を 8本→2本に下げた。
補給2本(=200)は「そのウェーブをうまく捌いたときの消費」とほぼ同じで、
**うまく撃てば収支トントン、雑に撃つと備蓄が減る**という形になる。

### 解析がモデル化していないこと（意図的）

ボルトは弾速(44→205m/s)・射程(22→92m)・貫通(0→4)も上げるが、
この解析は**命中率を一切考慮していない**。したがって表の「高速個体にボルトは無価値」は
「当たる前提なら無価値」という意味で、実際には 7.4m/s で回り込む相手に
58m/s の遅い弾を当てるのは難しい。ボルトの価値はここでは過小評価されている。
モデルを複雑にするより、過小評価のまま安全側に倒してある。

---

## 6. ウェーブ進行 — 完了 (09/17)

- インターミッション(初回3.4秒 / 以降5秒) → 0.55秒ごとに1体ずつ投入 → 全滅で次へ
- **一度に出さず少しずつ投入する**（同時存在上限22）。一斉に出すと「一発大技」の局面になり、
  持続的なリソース判断という設計が崩れるため
- 投入順は種別の残数が多い方から取る round-robin。ウェーブの開幕が装甲だけになるのを防ぐ
- ウェーブクリアで予備電池 +2本。最終ウェーブのクリアだけは補給せず `won`
- HUD の LEFT は「フィールド上の数 + 未投入の数」

敗北は2系統: HP 0 (`BODY BURNED OUT`) / 電池枯渇 (`OUT OF BATTERIES`)。
リザルトには **AVG COST / SHOT** を出している。撃ち分けができていれば一桁、
フルチャージ連打なら 100 に近づく。プレイヤーが自分の雑さを数字で見られるように。

リスタートは `runId` を増やし、`<Player key={runId}>` で剛体だけ作り直す。
Physics ごと作り直すと wasm ワールドの再初期化でひっかかるため、
プールと director は `useEffect` で明示的にクリアする。

### verify に足した確認

- `19-wave-one`: ウェーブ1の総数(7)が「場にいる数＋未投入」と一致し、少しずつ出ていること
- `20-wave-clear`: **実際に撃って**ウェーブ1を殲滅し、director が進み、予備が 8→10 になること
- `21-run-cleared`: 最終ウェーブ突破で `won` に入り、最後だけ補給されないこと
  （26体と 5fps で殴り合うのは検証の目的ではないので、director の状態を直接進めて
  終了遷移だけを見ている）

---

## 7. 絵づくり — 完了 (09/17)

### Bloom が設計の中心

エミッシブは全て 1.0 を大きく超える値で書き、砂や鉄は 1.0 を超えないようにしてある。
Bloom を1パス通すだけで「電気だけが強く光り、地面と鉄はマットなまま」という
コントラストが成立する。ブリーフの絵の要求は、実質この1パスで満たしている。

ベースは意図的に暗く戻した（ambient 0.85→0.34、hemisphere 1.15→0.5、vignette 0.85）。
Bloom を足した直後は全体が明るく眠くなっていた（mean 24→42）ので、
**明るさは Bloom に任せて、素の陰影は暗いまま**にしている。

### パフォーマンス（実測に基づく判断）

SwiftShader 実測で Bloom を入れた瞬間 3.5fps → **1.8fps** に落ちた。
やったこと:

1. **アリーナをインスタンス化**。壁28・鉄骨22・地面の欠片170・遠景のメサとやぐら —
   個別メッシュだと敵が1体も出る前に約250ドローコール。
   静的なものは1形状1ドローにまとめた（`StaticInstances`）。コライダーは
   単一の fixed ボディにまとめてある。
2. **MSAA を切った**。Bloom パスの上に載せると単体で最も高い。
   フラットシェーディングのローポリなので欠点が目立ちにくい。
3. **解像度の自動調整**（`PerformanceMonitor`）。Bloom は fill-rate 律速なので、
   追いつかない環境では**ピクセル数を減らしてアップスケールする**。
   絵の根幹であるエフェクトを切るより良い。初期値は 0.8 から始めて、
   余裕がある環境で 1.0 へ上げる（高い方から始めてカクつきながら下げるのは避ける）。

結果 3.3fps まで回復（= Bloom 導入前と同等）。実機GPUでは全く問題にならない範囲だが、
この調整は**低スペック環境の実プレイヤーにもそのまま効く**。

### ゲームフィール

- 撃破時ヒットストップ（装甲 0.075s / 雑魚 0.035s、時間を 0.22倍）。
  ヒットストップは**実時間で計測**し、それ以外を縮めた時間で動かす。
- 銃口のポイントライトは**常にマウントしたまま intensity 0**にしている。
  ライト数が変わるとマテリアルのシェーダが再コンパイルされ、戦闘中に引っかかるため。
- カメラシェイクは減衰を平滑化の**後**に足す（平滑化と喧嘩しないように）。

### 検証で見つかった実バグ

- **フレームより短いキー入力が落ちていた**。`keys.has('KeyR')` を毎フレーム
  ポーリングしていたので、3fps の環境では押して離すまでが**2フレームの間に
  収まってしまい、リロードが発生しなかった**。keydown をラッチして
  1回だけ消費する方式に変更（`consumePress`）。フレームレートに関係なく
  入力が消えない。実機でも「素早いタップが無視される」を防ぐ。
- **感電の検証がカメラの微調整で壊れた**。カメラ高さを変えたら弾道が変わり、
  密集の隙間を抜けるようになった。検証側を「敵を凍結して狙いを合わせてから撃つ」
  に変え、**弾道の偶然に依存しない**ようにした。
  凍結スイッチ（`debug.freezeEnemies`）は leva パネルにも出す予定のもの。

---

## 8. サウンド — 完了 (09/17)

Web Audio で実行時に合成。音声ファイルもフェッチもライセンスも無い。
使う素材は3つだけに絞ってある:

- **バンドパスしたホワイトノイズ** → 電気的なもの全部（発射、着弾、感電、跳弾）
- **矩形／鋸波** → 機械的なもの（撃発、ウェーブ通知）
- **サイン波の下降スイープ** → 着弾と被弾の重さ

**チャージ音は2本の常時鳴りっぱなしのループ**で、毎フレーム V と A で変調する。
ボルトは音程（150Hz → 1400Hz）、アンペアはバンドパスの中心と Q（＝ざらつき）。
発射音そのものも `ShotSpec` から作るので、**撃った音でいくら使ったかが分かる**。

AudioContext は DRAW / AGAIN のクリックハンドラの中で作る（ユーザー操作が必要）。
`initAudio` は全体 try/catch で、失敗しても `audio.ready=false` になるだけで
ゲームは動く。

1フレームに大量の音が重なるのを防ぐため 0.1秒あたり24ボイスの予算制。
M キーでミュート。

`22-audio` が「AudioContext が起動していること」「発射で実際に音がスケジュールされること」
「1発が複数ボイスであること」「M でミュートが切り替わること」を確認している。

---

## 9. 調整パネル・タイトル・操作説明 — 完了 (09/17)

- **leva パネル**はバッククォートで開閉。既定では非表示。
  各コントロールは config オブジェクトに直接書き込むので、
  リロードなしでその場で効く。`log economy` ボタンは verify が assert しているのと
  同じ解析表を出す（PROGRESS.md の数字はこれで詰めた）。
  `spawnPaused / freezeEnemies / godMode / infiniteBattery` も同じパネルから触れる。
- **タイトル画面**に操作一覧と「消費は V×A」の説明を出した。
  操作一覧はポーズ画面と同じコンポーネントを共有している。
- **ポーズを追加した**。ポインタロックを失う（Esc、alt-tab）と、
  それまでは**狙えないのにゲームだけ動き続けていた**。
  ロックが外れたらワールドを止め、クリックで復帰する。
  `23-pause-on-lost-lock` が「止まること」「オーバーレイが出ること」を確認している。

---

## 最終検証: 実際に通しで遊ばせた (09/17)

静的解析（`economy.ts`）は「紙の上で最適に撃てば足りる」ことしか言わない。
実際に6ウェーブ通しで遊ばせるスクリプト（`scripts/dev/playthrough.mjs`）を書いて、
2つの戦略を比較した。**god mode を有効**にしてあるのは、
検証したいのが「電池が足りるか」であって「スクリプトが回避できるか」ではないため。

| 戦略 | 結果 |
|---|---|
| **lazy**（毎回フルチャージ） | **ウェーブ4で `OUT OF BATTERIES`**。23発、全て100 units |
| **picky**（敵種別に撃ち分け） | **完走 (`won`)**。91体撃破／81発／平均 20.7 units、予算の約66%で終了 |

どちらも**コンソールエラー0件**（1.7分／4.0分の連続実行）。

### この結果を受けた調整

picky 側は当初 **予備電池1本**しか残さずギリギリの勝利だった。
bot は狙いを外さず、被弾もしない（god mode）。実際の人間は外すし殴られるので、
そのままでは厳しすぎる。ウェーブ補給を 2本 → **3本**に上げた（予算 2400 → 2900）。

- picky: 予算の 66% で完走 → 外す・溜めすぎるぶんの余裕ができた
- lazy: それでも **1.54倍の予算超過**で、ウェーブ4で確実に尽きる

なお bot は「敵種別に正しい弾を選ぶ」だけで、**群れをまとめて撃つことはしない**
（一番近い1体に向けて撃つ）。だから平均 20.7 units と、理論最適の 6.8 units/体より
かなり高い。**群れを固めて感電で処理する**という上達の余地が、
bot の性能と理論値の差として残っている。これは意図した形。

### ここまでで verify が固定していること

24シナリオ / 81アサーション。内訳:

- 描画: コンソールエラー0件、輝度標準偏差、敵を出す前後のピクセル差分
- 操作: チャージ、「押している間は撃たない」、離す順序不問、フレーム落ちしても入力が消えないこと
- 収支: 3種の弾の電池コスト（±0.4 units）、リロードの損、出力低下、電池切れ敗北
- 撃ち分け: 装甲が低電圧を弾くこと、高電圧が抜くこと、1発の感電が群れ5体を消すこと
- 設計ルール: (V,A) 平面の総当たり解析（3つの最適解が別物であること、
  フルチャージ連打が予算を超えること、撃ち分ければ余裕が残ること）
- 進行: ウェーブ構成、殲滅→補給→次へ、最終ウェーブでクリア
- その他: 音が実際に鳴ること、ポインタロック喪失でポーズすること、調整パネルの開閉

## 着手しなかったもの（記録のみ）

思いついたが、指示どおり着手せず記録だけしたもの:

- 弾の軌跡に沿った残光（トレイルメッシュ）。現状は伸びたカプセル1つで代用
- 敵の死体・焦げ跡の残留
- 装甲個体の部位破壊（装甲板を剥がすと閾値が下がる）
- 感電の連鎖（敵から敵へ段階的に伝播させる）。現状は着弾点から一斉に届く
- スコアのランキング保存（セーブデータは「やらないこと」に含まれる）

---

## CI (09/18)

`.github/workflows/ci.yml` で push / PR ごとに `npm run build` と `npm run verify` を回す。

- **ブラウザのダウンロードより先にビルドする**。型エラーなら Chromium を取得する前、
  1分ほどで落ちる
- Chromium は `package-lock.json` のハッシュでキャッシュ（ブラウザのビルド番号は
  固定した playwright のバージョンと一致している必要がある）
- **スクリーンショットは成功・失敗にかかわらず成果物として上げる**。
  この検証の失敗は「何が描かれていたか」を見ないと原因が分からないため
- `concurrency` + `cancel-in-progress`: verify は数分かかるので、
  新しい push が来たら古い実行は打ち切る
- タイムアウト 35分（実測は約8分）

### 検証環境の制約について

このコンテナはネットワークポリシーで `cdn.playwright.dev` と Microsoft のミラーを
遮断している（ゲートウェイが CONNECT に 403）。そのため
`npx playwright install` のステップだけはローカルで実行できなかった。
代わりに**それ以外の全経路を CI と同じ状態で検証した**:

- コンテナが持つ `PLAYWRIGHT_BROWSERS_PATH` と `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD` を外す
- ブラウザを CI と同じ既定の場所（`~/.cache/ms-playwright`）に置く
- その状態で `npm ci` → `npm run build` → `npm run verify` を通す → 24シナリオ全通過

つまり未検証なのはダウンロード1ステップだけで、これは GitHub Actions の
ランナーでは制限がかからない。

### 最初の CI 実行で落ちた（09/18）

`playwright install` は問題なく通った（Chromium は `~/.cache/ms-playwright` に取得された）。
シナリオ 01〜10 も通った。落ちたのは **`11-player-damage`** で、
「敵がプレイヤーに接触するのを45秒待って、届かなかった」。

ログの状態から時間の進み方が逆算できた。director は 3.4秒のインターミッションの後
0.55秒ごとに1体投入し、7体中5体まで出ていた → **ゲーム内時間で約6.2秒**。
これに要した実時間は約50秒。つまり **CI ではゲーム内時間が実時間の約12%でしか進んでいない**
（手元の約8分の1の速さ）。

原因はテスト側の設計ミス。**待っている対象はすべてゲーム内時間の事象**
（チャージが溜まる、ウェーブが湧く、敵が砂地を歩いて来る）**なのに、
タイムアウトは実時間で書いていた**。両者の比はレンダラの速度次第で、
手元で調整した値が CI で通る保証はどこにもなかった。

対処:

1. **`11-player-damage` から「歩いて来るのを待つ」依存を外した**。
   群れをプレイヤーに接触した位置に直接置く（10/16/17 で既にやったのと同じ手）。
   このシナリオが確認したいのは近接ダメージと無敵時間であって、移動時間ではない。
   横断にかかる時間は `09-enemies` と `20-wave-clear` が見ている。
2. **待ち時間の予算を実測比に合わせて広げた**（`waitForState` 45秒→180秒ほか）。
   09 / 19 / 20 も同じ理由でぎりぎりで通っていただけなので、次に落ちるのはそれらだった。
   予算を広く取っても、条件が満たされれば即座に返るので通常時のコストはゼロ。
   効くのは本当にハングしたときだけ。

修正後、手元で 24シナリオ全通過（6分30秒）。シナリオ11は待ちが消えたぶん速くなった。

### 2回目の CI 実行: `12-enemies-on-screen` が落ちた（09/18）

24中23が通り、残る1つが「敵を出す前後のフレーム差分」だった。
**diff 1.35 に対して閾値 1.8**。敵は7体とも出ていて、距離 0.63m まで来ていた。

最初の仮説は「スポーン演出（`emerge`、ゲーム内0.45秒）が終わっていないから小さい」。
**これは実測で否定された**:

| 状態 | diff |
|---|---|
| 出現途中 (emerge 0.44) | 2.201 |
| 完全に出現 (emerge 1.0) | **1.848** |

完全に出た方が**小さい**。つまり emerge は原因ではなかった。

本当の原因は**シナリオ12が敵を凍結していなかったこと**。敵は待っている間プレイヤーに
歩いて近づくので、画面を占める面積は「どれだけゲーム内時間が経ったか」で決まる。
手元では900msの実時間で十分なゲーム内時間が流れて敵が目前まで来る（diff 2.9〜3.4）。
CI では同じ900msがほとんどゲーム内時間にならず、敵は遠いまま（diff 1.35）。
**計測対象が「描画されているか」ではなく「どれだけ時間が経ったか」になっていた。**

対処: **敵を凍結し、近い位置に密に10体置き、出現完了を状態で待ってから計測する**。
これで形状が完全に決定的になり、残る変動はレンダリング解像度だけになる。
この配置での実測は **2.883**（閾値1.8に対して1.6倍の余裕）。

### ついでに見つかった `freezeEnemies` の不具合

凍結を実装するとテストがハングした。`debug.freezeEnemies` が
`updateEnemies` の呼び出しごと飛ばしていたため、**凍結した敵は emerge が永遠に終わらない**。
凍結は「位置を止める」ことであって「実体を停止する」ことではないので、
判定を `updateEnemies` の内側、タイマー更新の後ろに移した。
出現演出とヒットフラッシュは動き続け、操舵と攻撃だけが止まる。

---

## 公開 (09/18)

`.github/workflows/deploy.yml` で main への push ごとに GitHub Pages へデプロイする。

- `vite.config.ts` の `base: './'`（相対パス）のおかげで、プロジェクトサイトの
  `/AAgunmandemo/` というサブパスでもそのまま動く。
  **これは推測ではなく実測で確認した**: `dist` をサブディレクトリに置いて配信し、
  ブラウザで開いて「`playing` に入る・ポインタロックが取れる・コンソールエラー0件」
  を確認している（`scripts/dev/subpath.mjs`）。
- `configure-pages` の `enablement: true` で、初回に設定画面を触らなくても
  Pages が有効になる。
- `concurrency: pages` は `cancel-in-progress: false`。
  公開サイトは1つしかないので、途中で打ち切るよりキューに積む方が安全。

### リポジトリの公開設定について

GitHub Pages は **public リポジトリなら無料プランで使える**が、
private だと Pro / Team / Enterprise が要る。このリポジトリは private だったので、
公開範囲の変更という**持ち主の判断が要る事項**として確認を挟んだ。
結論は「public にする」。公開設定の変更自体は私からは行っていない。

---

## 公開後: 真っ黒画面の報告と、失敗を可視化する仕組み (09/18)

デプロイは成功していたが、実機（Brave）で開くと**画面が完全に真っ黒**という報告。
配信物は正しかった（Pages 成果物 1.14MB ＝ フルバンドル相当）。
同じバンドルがヘッドレスでは動くので、差分は環境（ブラウザ・GPU・拡張）側。

**再現できない不具合を推測で直すのは筋が悪い**ので、
まず**アプリ自身に失敗を報告させる**ようにした。真っ黒だった理由は単純で、
`<Canvas>` の中で例外が出ると React ツリーごと落ち、`#root` が空になり、
body の背景色（`#07060a`）だけが残るため。手がかりがゼロになる。

入れたもの:

1. **`index.html` のブートフォールバック** — `#root` が空のままでも文字が出る。
   `window.onerror` / `unhandledrejection` を拾ってスタックを画面に出し、
   10秒経ってもマウントされなければ `STALLED` と表示する。
   バンドルがそもそも実行されない場合もこれで分かる。
2. **React エラーバウンダリ** — Canvas 配下の例外を捕まえて `CRASHED` とスタックを表示。
3. **WebGL の事前チェック** — three.js の奥で落ちる前に自前で
   `getContext('webgl2'/'webgl')` を試し、取れなければ
   `WEBGL UNAVAILABLE` と**対処方法**（Brave ならシールドを切る等）を出す。

### 仕込んで確認した

verify と同じやり方で、意図的に壊して表示されることを確認:

| 仕込み | 結果 |
|---|---|
| `getContext('webgl*')` が null を返すようにする | `WEBGL UNAVAILABLE` が表示、canvas なし |
| 通常 | 従来どおりタイトル画面、fatal パネルなし |

これで「真っ黒で何も分からない」状態は起きなくなり、
次に同じことが起きたら**画面に原因が出る**。

### 診断の穴: リソース読み込み失敗を拾えていなかった (09/18)

真っ黒対策を入れた直後の実機報告は **`STALLED`（エラー表示なし）**。
つまり「例外が出た」のではなく「**JSモジュールがそもそも実行されていない**」。
HTML は新しくなっていた（このブート画面自体が新規追加分）ので、
配信は効いていて、バンドルの読み込みだけが失敗していることになる。

ここで自分の診断にバグがあった。

```js
window.addEventListener('error', handler)   // ← script の 404 を拾えない
```

**リソース（script / img / link）の読み込み失敗イベントはバブルしない**ので、
capture フェーズで登録しないと window では捕まらない。
つまり「バンドルが404」という最もありそうな失敗が、
ちょうど「STALLED・原因表示なし」として出ていた。診断が沈黙していた。

直したもの:

1. `addEventListener('error', handler, **true**)` にして、
   `e.target.src` があればそのURLを `FAILED TO LOAD` として表示する
2. それでもマウントされない場合、**バンドルのURLを自分で `fetch` して
   HTTPステータスと content-type を画面に出す**。
   - 404 → `BUNDLE HTTP 404`（サーバに無い）
   - 200 なのに動いていない → `BUNDLE LOADED BUT DID NOT RUN`（拡張機能等による遮断）
   - fetch 自体が失敗 → `BUNDLE UNREACHABLE`

### 仕込んで確認した

| 仕込み | 結果 |
|---|---|
| JSだけ 404 を返すサーバで配信 | `FAILED TO LOAD` ＋ 実URL を即座に表示 |
| 通常 | マウント成功、タイトル画面 |

教訓: **「エラーを表示する」仕組みを入れたら、
それが本当に発火することを壊して確かめるまで信用してはいけない。**
verify 本体には最初にやったのに、この診断コードには一度やらずに出してしまった。

### 真っ黒の真因: Pages の配信元がブランチだった (09/18)

改善した診断が出した答えは:

```
FAILED TO LOAD
https://vocabuki-io.github.io/src/main.tsx
```

`/src/main.tsx` を指すのは**ビルド前のソースの `index.html`** だけ。
ビルド後の `dist/index.html` は `./assets/index-xxxx.js` を指す。
つまり **GitHub Pages が Actions の成果物ではなくブランチ（main のルート）を配信していた**。
リポジトリ直下のソース `index.html` がそのまま返り、`/src/main.tsx` は
（絶対パスなのでドメイン直下を見に行き）404 になり、何も起動しない。
最初の「真っ黒」からずっとこれが正体だった。

デプロイ自体は毎回 success を返していた。**成果物のアップロードは成功しており、
ただ公開サイトがそれを見ていなかった**ので、ワークフローからは成功に見えていた。

対処: 配信元を `GitHub Actions` に変更する（リポジトリ設定側）。

そしてワークフローに **公開後のスモークテスト**を追加した。
デプロイ直後に公開URLを実際に取得し、`assets/index-*.js` を参照しているか確認する。
していなければ失敗させ、`/src/main.tsx` を参照している場合は
「Pages の Source がブランチになっている」と名指しで出す。

これがあれば、**「デプロイは緑なのにサイトは真っ黒」という状態が成立しなくなる**。
今回いちばん時間を溶かしたのがまさにそれだった。

ローカルで両方向を確認済み: `dist/index.html` に対しては PASS、
ソースの `index.html` に対しては FAIL＋原因の名指し。

---

## 前後が反転していた (09/18)

実際に遊んでもらって発覚: **W で後退、S で前進**。

原因は `Player.tsx` の移動ベクトル。`W` は「前方は視点空間の -Z」なので `iz = -1` を立てる。
つまり**前方成分は `-iz`** なのに、コードは前方ベクトルに `nz`（= `iz/len`、すでに負）を
そのまま掛けていた。二重に符号が反転して前後が入れ替わっていた。

yaw=0 で検算すると `W` → `wz = +1`（前方は -Z なので後退）。
`A`/`D` は `nx` 側の項が正しかったので無事で、報告が「W と S だけ」だったのと一致する。

修正は前方成分を `-iz / len` として明示しただけ。yaw 0 / 90 / 180 で
`W=前 / S=後 / D=右 / A=左` になることを確認した。

### 本当の失敗は、テストが無かったこと

verify は24シナリオあったが、**移動方向を一度も検証していなかった**。
チャージも発射も敵も電池もウェーブも見ていたのに、
「W を押したら前に進む」という最も基本的なことに穴が空いていた。だから出荷された。

`25-movement-directions` を追加。カメラの向きに対して実際の変位を射影し、
`W` が前方成分 > 0.85、`S` が < -0.85、`D`/`A` が右成分 ±0.85 であることを確認する。
**世界座標軸ではなくカメラ基準**なので、どの方位を向いていても成立する。
移動量は時間ではなく**距離で待つ**（ゲーム内時間と実時間の比はレンダラ次第、の教訓）。

### 仕込んで確認した

元の反転した式に戻して `25` を実行 → **`alongForward` が W で -0.9995、S で +0.9995**、
つまり真逆であることを検出して失敗（`D`/`A` は通過し 3/5）。
修正版では 5/5。新しいテストが本当にこのバグを捕まえることを確認した上で入れている。

あわせて `VERIFY_ONLY=25` のようにシナリオを絞って実行できるようにした（反復が速くなる）。

---

## 三種類の弾にした (09/18)

遊んだ上での要望: **V は必ず貫通・直進・高速で、溜めるほど弾速が上がる。
A は丸い遅い球で、当たると爆風が広がり、溜めるほど爆風が大きい。V×A は極太ビーム砲。**

それまでの実装は V と A が「一本のスライダーの上の点」でしかなかった。
V を上げれば速く遠く貫通し、A を上げれば痛く広く痺れる、という連続量。
撃ち分けは成立していたが、**撃った瞬間に何を撃ったのか見て分からない**。

### どのボタンを握っていたかで別の武器にする

`shotKind(vRaw, aRaw)` を追加した。チャージ量ではなく**押していたボタン**で決まる:

- `volt`（右だけ）… 常時貫通の槍。`voltPierceBase 3` + 満タンで `+5`。爆風なし
- `amp`（左だけ）… 速度 `×0.55` の球。爆風 `×1.35`、貫通 0
- `beam`（両方）… 速度 `×1.7`、貫通 `4+`、爆風もある。両方のコストを払うので当然

実測値: volt 槍 {速度 205, 貫通 8}、軽いタップでも {81.8, 貫通 3}。
amp 球 {速度 33.1, 貫通 0, 爆風 8.78}、タップで {33.1, 0, 1.76}。
beam {速度 348.5, 貫通 9, 爆風 6.5}。

「Vは**必ず**貫通」という指定なので、貫通は `floor(v * pierceMax)` ではなく**下駄を履かせた**。
タップでも 3 体抜く。逆に A は貫通 0 に固定した。爆ぜるのが仕事なので。

### 見た目を三つに分けた

`Bolts.tsx` を、種類ごとの instanced mesh 3 レイヤに書き直した。

- 槍: 6 角シリンダー、半径 0.093、長さは弾速に比例（最大 7m）
- 球: 球体、半径は爆風半径に比例。脈動と自転を入れて「生きている」感を出す
- ビーム: 10 角シリンダー、半径 0.42（槍の 4.5 倍）、長さ最大 9m

爆風用に `blasts` プールを新設し、**ワイヤーフレームの二十面体**で描く。
面で描くと単なる白い塊になるが、線なら「膨張している球」として読める。

### ハローを絶対値で頭打ちにした

最初、光暈を `半径 × 3.2` にしていたら、ビームの光暈が半径 2.2m・長さ 10m になり、
**撃った瞬間に画面が白い壁で埋まって、撃った先が見えない**。
満タンの V は FOV 26 まで寄っているので、なおさら効く。

`半径 + min(半径 × 2.2, 0.3)` に変えた。細い槍の見た目はほぼそのままで、
ビームだけが頭打ちになる。ビームの芯も 0.68 → 0.42 に絞った。
それでも槍の 4.5 倍あるので「極太」ではある。

### 撮るために時間を遅くした

槍は 205 m/s で、9m 先の敵まで **44ms** で着弾する。スクリーンショットでは絶対に捉えられない。
`debug.timeScale` を足して（leva からも触れる）、撮影時だけ 0.12 で回すようにした。
ゲーム本体は 1 のまま素通り。スローモーションはエフェクト調整にもそのまま使える。

`scripts/dev/shots.mjs` で 4 枚撮る。爆風の一枚だけは**時間ではなく `enemyHits > 0` を待つ**。
どのフレームで着弾するかはレンダラの機嫌次第で動くので、時間で待つと空振りする。
（実際、最初の版は 3 回撮って 3 回とも飛翔中の球が写った。）

撮影中に球が消える現象を追ったら、**装甲持ちに弾かれていた**だけだった。
A だけの弾は電圧が `voltMin` なので装甲を抜けない。仕様通りの挙動。
球の撮影対象を swarm に変えたら、狙い通り 3 体まとめて消し飛んだ。

### 経済の検証を「数字」から「意味」に変えた

`minOptimumSpread >= 0.3` が 0.22 に落ちて落第した。
原因は退化ではなく**この機能そのもの**で、爆風 1.35 倍のおかげで swarm が
A 0.40 ではなく 0.30 で片付くようになったから、最適値同士が近づいた。

閾値を黙って下げるのは、テストを現実に合わせて緩めるだけなので、**測る対象を変えた**。
今は「装甲 → beam、群れ → amp、走者 → amp、かつ `swarm.a - runner.a >= 0.15`」を見る。
どの弾を選ぶべきかという**設計意図そのもの**を検証している。

現在の最適解: 装甲 → beam (v 0.48, a 0.68, 40.01/体)、
群れ → amp (v 0.00, a 0.30, 1.13/体、5 体巻き込み)、走者 → amp (v 0.00, a 0.08, 2.84/体)。
満タン連打は予算の 1.54 倍で相変わらず破産する。

### テスト

`26-shot-kinds`（15 チェック）を追加。速度・貫通・爆風を種類ごとに確認した上で、
**実際にゲーム内で装甲 4 体を 5/7/9/11m に縦一列に並べ、満タンの槍を 1 発撃つ**。
`boltsSpawned === 1`、`enemyHits >= 4`、`arcHits === 0` を要求する。
一発が 4 体を貫いたこと、かつ爆風で当たったのではないことが、これで同時に言える。

最初 14/15 だった。4 体のうち 2 体にしか当たらず `worldImpacts: 1`。
**射線上に鉄骨が立っていた**（最内周のリングが 13〜17m）。列を 5/7/9/11m に寄せて 15/15。

全 26 シナリオ・115 チェック通過。
